sugar_amount = 2
print(f"Initial Sugar : {sugar_amount}")

sugar_amount = 12
print(f"Initial Sugar : {sugar_amount}")

#Numbers are Immutable then how it changes remember always to track with the identity not by value

print(f"Id of 2 : {id(2)}")     #Both of them have different Id so they are Immutable 
print(f"Id of 12 : {id(12)}")